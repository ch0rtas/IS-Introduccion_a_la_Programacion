#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define PI 3.1416

int main (){

    /*
    int num1, num2, resultado;
    char letraUser = 'a';
    float altura = 0.5f;    // printf("%f\n", b);
    double precio = 0.5;    // printf("%fl\n", a);
    printf("Decimal: %d\n", numero);
    printf("Hexadecimal: 0x%x\n", numero);
    printf("Octal: 0%o\n", numero);
    resultado = num1 << 3;      // multiplicar por 2 elevado a 3 - desplazar 3 bits a la izq
    resultado = num1 >> 2;      // dividir por 2 elevado a 3 - desplazar 3 bits a la der
    printf ("Entero vale %d y ocupa la direccion %x \n",Entero,&Entero);
    fabs(num1 - num2) // sirve para sacar el valor absoluto
    pow(num1,num2) // num1 elevado a num2
    %02.3X dos unidades y tres decimanles
    */

    int x;

    switch (x){
        case1:
        case2: printf("Hola mundo 2");
            break;
        case3: printf("Hola mundo 3");
            break;
        default: printf("Hola mundo X");
    }

    printf("\n");
    system ("pause");
    return 0;
}